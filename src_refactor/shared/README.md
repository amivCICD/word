# Refactor Workspace

This folder is reserved for the new isolated implementation.

- `single_player/` contains new single-player entry code.
- `multi_player/` contains new multiplayer entry code.
- `shared/` is for modules intentionally shared by the refactor versions only.
